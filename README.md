# basic_django

## Installation
`sudo apt-get python3 django-admin`

#### Start Project
1. Create Project
`django-admin startproject basic_django`
2. Run Server
`python manage.py runserver`
